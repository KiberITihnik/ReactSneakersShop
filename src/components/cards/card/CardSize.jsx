import React from 'react';

const CardSize = ({ size }) => {
    return (
        <div>
            <li className="card-size-sneakers-child">{size}</li>
        </div>
    );
};

export default CardSize;
